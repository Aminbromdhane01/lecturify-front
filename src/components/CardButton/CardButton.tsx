import { palette } from '@/theme/palette'
import { Button } from '@mui/material'
import React from 'react'
import { StyledButton } from './CardButton.style'

const CardButton = () => {
    return (
        <StyledButton>Get This Book</StyledButton>
    )
}

export default CardButton
