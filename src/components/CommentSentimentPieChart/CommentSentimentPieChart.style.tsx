'use client'
import { Paper } from "@mui/material";
import { styled } from '@mui/system';

export const ChartContainer = styled(Paper)(({ theme ,color }) => ({
    display : 'flex' , 
    justifyContent :'center' 
}))
